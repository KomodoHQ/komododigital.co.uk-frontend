import React, { ReactNode } from 'react';

interface Props {
  data: any;
}

const VCard: React.SFC<Props> = ({ data }) => {
  return (
    <div>
        VCard
    </div>
  );
};

export default VCard;
