import React, { ReactNode } from 'react';

interface Props {
  data: any;
}

const Services: React.SFC<Props> = ({ data }) => {
  return (
    <div>
        Services
    </div>
  );
};

export default Services;
