export interface PageMeta {
  title: string;
  description: string;
}

export interface SchemaDataType {
  title: string;
  headline: string;
  author?: string;
  date: string;
  body: string;
  imageURL?: string;
  url: string;
  logo?: string;
}
