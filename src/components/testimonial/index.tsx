import React, { ReactNode } from 'react';

interface Props {
  data: any;
}

const Testimonial: React.SFC<Props> = ({ data }) => {
  return (
    <div>
        Testimonial
    </div>
  );
};

export default Testimonial;
