import React, { ReactNode } from 'react';

interface Props {
  data: any;
}

const GridItem: React.SFC<Props> = ({ data }) => {
  return (
    <div>
        GridItem
    </div>
  );
};

export default GridItem;
