import React, { ReactNode } from 'react';

interface Props {
  data: any;
}

const QuadGrid: React.SFC<Props> = ({ data }) => {
  return (
    <div>
        QuadGrid
    </div>
  );
};

export default QuadGrid;
