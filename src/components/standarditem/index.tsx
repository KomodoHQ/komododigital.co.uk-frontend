import React, { ReactNode } from 'react';

interface Props {
  data: any;
}

const StandardItem: React.SFC<Props> = ({ data }) => {
  return (
    <div>
        StandardItem
    </div>
  );
};

export default StandardItem;
