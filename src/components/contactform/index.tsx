import React, { ReactNode } from 'react';

interface Props {
  data?: any;
}

const ContactForm: React.SFC<Props> = ({ data }) => {
  return (
    <div>
        ContactForm
    </div>
  );
};

export default ContactForm;
