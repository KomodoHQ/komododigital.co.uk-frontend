import React, { ReactNode } from 'react';

interface Props {
  data: any;
}

const LegalFooter: React.SFC<Props> = ({ data }) => {
  return (
    <div>
        LegalFooter
    </div>
  );
};

export default LegalFooter;
