import React, { ReactNode } from 'react';

interface Props {
  data?: any;
}

const BlogPost: React.SFC<Props> = ({ data }) => {
  return (
    <div>
        BlogPost
    </div>
  );
};

export default BlogPost;
