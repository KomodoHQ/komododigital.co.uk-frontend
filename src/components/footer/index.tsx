import React, { ReactNode } from 'react';

interface Props {
  data: any;
}

const Footer: React.SFC<Props> = ({ data }) => {
  return (
    <div>
        Footer
    </div>
  );
};

export default Footer;
