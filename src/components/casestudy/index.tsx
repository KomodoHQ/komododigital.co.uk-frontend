import React, { ReactNode } from 'react';

interface Props {
  data: any;
}

const CaseStudy: React.SFC<Props> = ({ data }) => {
  return (
    <div>
        CaseStudy
    </div>
  );
};

export default CaseStudy;
