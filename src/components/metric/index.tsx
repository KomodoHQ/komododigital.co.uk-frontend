import React, { ReactNode } from 'react';

interface Props {
  data: any;
}

const Metric: React.SFC<Props> = ({ data }) => {
  return (
    <div>
        Metric
    </div>
  );
};

export default Metric;
